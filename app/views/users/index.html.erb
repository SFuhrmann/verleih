<% if user_signed_in? %>
		<% if current_user.role == "admin" || current_user.role == "mitarbeiter" %>

<h1>Alle User</h1>

<table>
  <tr>
    <th>User</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.id %></td>
    <td><%= user.name %></td>
    <td><%= link_to 'Zeigen', user %></td>
    <td><%= link_to 'Loeschen', user, :confirm => 'Sicher?', :method => :delete %></td>
  </tr>
<% end %>
</table>


<% end %>
<% end %>